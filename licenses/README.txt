Bundled runtime license notices

These notices come from the Microsoft.NETCore.App.Runtime.win-x64 10.0.12
and Microsoft.WindowsDesktop.App.Runtime.win-x64 10.0.12 NuGet packages.
Keep them with the redistributed runtime. Refresh these notices when
updating the pinned runtime. They apply to the dependencies identified
inside the files, not to Monitor Sync's own source code.
